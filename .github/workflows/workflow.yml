name: CI workflow

on:
  push:
    branches:
      - "main"
  pull_request:
    types: [opened, reopened]
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    container: python:3.9-slim
    
  